name: Parallel jobs

on:
  push:
    branches: [workflow-planner]

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo ${{ secrets.MY_SECRET }} > token.txt
          cat token.txt

      - run: cat token.txt
