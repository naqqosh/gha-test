name: Learn

on:
  pull_request:
    branches:
      - main

jobs:
  do_pr_job:
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "I think you opened a pull request to branch main, am I true?"
