name: "Sevices: Postgres"

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Action dependencies'larini o'rnatish
        run: |
          cd .github/actions/salom
          npm install

      - name: O'z actionimni ishlatish
        uses: ./.github/actions/salom
        with:
          ism: "Bobur"

      - run: echo "${{ steps.salom.outputs.natija }}"
