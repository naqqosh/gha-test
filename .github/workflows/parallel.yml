name: Parallel jobs

on:
  push:
    branches: [workflow-planner]

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - run: error_ber

  job2:
    needs: job1
    runs-on: ubuntu-latest

    steps:
      - name: shartli ishla
        if: github.ref_name == 'workflow-planner'
        run: echo "demak siz workflow-planner'ga push qildiz, bu job1 dan keyin ishladi"

  job3:
    runs-on: ubuntu-latest

    steps:
      - run: echo "bu ishlar ekan"
