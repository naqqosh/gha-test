name: "Sevices: Postgres"

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          ls -la
          ls -la .github
          ls -la .github/actions/salom

      - uses: ./.github/actions/salom
        with:
          ism: "Bobur"

      - run: echo "${{ steps.salom.outputs.natija }}"
