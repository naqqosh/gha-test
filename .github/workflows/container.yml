name: pnpm

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: echo "hello environment"

      - uses: ./.github/actions/setup-node
        with:
          node-version: "22"
